# CREATE
CREATE TABLE products(
  idx int NOT NULL AUTO_INCREMENT,
  prdName varchar(15) NOT NULL,
  prdPrice int NOT NULL,
  PRIMARY KEY (`idx`)
);

# INSERT
INSERT INTO calckiosk.products
(idx,
prdName,
prdPrice)
VALUES
(idx,
prdName,
prdPrice);